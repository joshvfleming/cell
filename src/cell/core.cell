;; cell core

(def =
  (=> (a b)
      (eq a b)))

(def zero?
  (=> (n)
      (= n 0)))

;; decrements the number
(def dec
  (=> (n)
      (- n 1)))

;; increments the number
(def inc
  (=> (n)
      (+ n 1)))

(def if
  (=> (c a b)
      (cond c a
            true b)))
      

