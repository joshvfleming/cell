/* Atom
 *
 * This is the atom representation class.
 */
cell.Atom = (function() {
  var Atom = function Atom(){};

  Atom.prototype.isAtom = function isAtom() {
    return true;
  };

  return Atom;
})();
